let current={};
async function api(path,opts={}){const r=await fetch(path,{headers:{'Content-Type':'application/json'},...opts});return r.json()}
async function init(){const h=await api('/api/health');document.getElementById('status').textContent=h.ok?'API Online':'API Error';document.getElementById('status').style.color=h.ok?'#77e3a8':'#f6c33b';if(!h.google_places_configured)document.getElementById('leadMessage').textContent='Google Places API key is not configured on the server yet.'}
function payload(){return {name:document.getElementById('name').value,category:document.getElementById('category').value,city:document.getElementById('city').value,website:document.getElementById('website').value,phone:document.getElementById('phone').value,notes:document.getElementById('notes').value}}
async function analyze(){current=payload();if(!current.name){alert('Business name required');return}const x=await api('/api/score',{method:'POST',body:JSON.stringify(current)});document.getElementById('score').textContent=x.score;document.getElementById('priority').textContent=x.priority+' priority';document.getElementById('reasons').innerHTML=x.reasons.map(r=>'• '+r).join('<br>')}
async function generateAI(){if(!current.name)current=payload();const x=await api('/api/gemini',{method:'POST',body:JSON.stringify(current)});document.getElementById('ai').textContent=x.ok?x.text:'AI: '+x.error}
function focusLeadFinder(){document.getElementById('leadFinder').scrollIntoView({behavior:'smooth'})}
async function searchLeads(){const category=document.getElementById('searchCategory').value.trim();const city=document.getElementById('searchCity').value.trim();const limit=Number(document.getElementById('searchLimit').value);if(!category||!city){document.getElementById('leadMessage').textContent='Enter both niche and city.';return}document.getElementById('leadMessage').textContent='Searching…';const x=await api('/api/leads/search',{method:'POST',body:JSON.stringify({category,city,limit})});if(!x.ok){document.getElementById('leadMessage').textContent=x.error||'Lead search failed.';return}document.getElementById('leads').textContent=x.count;document.getElementById('missing').textContent=x.leads.filter(l=>l.website_missing).length;document.getElementById('high').textContent=x.leads.filter(l=>l.website_missing).length;document.getElementById('leadMessage').textContent=`Found ${x.count} prospects. No-website leads are the first sales targets.`;document.getElementById('leadTable').innerHTML=x.leads.map(l=>`<div class="lead-row"><div><strong>${escapeHtml(l.name)}</strong><small>${escapeHtml(l.address||city)}</small></div><div>${l.website_missing?'<span class="badge hot">No website</span>':'<span class="badge">Website</span>'}</div><div>${l.rating?`★ ${l.rating} (${l.reviews||0})`:'—'}</div><button class="secondary small" onclick='loadLead(${JSON.stringify(l)})'>Analyze</button></div>`).join('')}
function loadLead(l){current={name:l.name,category:l.category,city:l.address||'',website:l.website||'',phone:l.phone||'',notes:`Google rating: ${l.rating||'N/A'}; reviews: ${l.reviews||0}`};document.getElementById('name').value=current.name;document.getElementById('category').value=current.category;document.getElementById('city').value=current.city;document.getElementById('website').value=current.website;document.getElementById('phone').value=current.phone;document.getElementById('notes').value=current.notes;analyze();window.scrollTo({top:document.body.scrollHeight/2,behavior:'smooth'})}
function escapeHtml(v){return String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
init();