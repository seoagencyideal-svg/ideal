let current={};
let scoreRequestInFlight=false;
let demoCount=0;

async function api(path,opts={}){const r=await fetch(path,{headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts});let data;try{data=await r.json()}catch(e){data={ok:false,error:`HTTP ${r.status}`}}if(!r.ok){throw new Error(data.error||data.detail||`Request failed (${r.status})`)}return data}
async function init(){try{const h=await api('/api/health');document.getElementById('status').textContent=h.ok?'API Online':'API Error';document.getElementById('status').style.color=h.ok?'#77e3a8':'#f6c33b';if(!h.google_places_configured)document.getElementById('leadMessage').textContent='Google Places API key is not configured on the server yet.'}catch(e){document.getElementById('status').textContent='API Error';document.getElementById('status').style.color='#f6c33b'}updateDemoCounter()}
function updateDemoCounter(){const el=document.getElementById('demoCount');if(el)el.textContent=String(demoCount)}
function payload(){const notes=document.getElementById('notes').value.trim();const ratingMatch=notes.match(/Google\s+rating\s*:\s*([0-9]+(?:\.[0-9]+)?)/i);const reviewsMatch=notes.match(/reviews\s*:\s*([0-9]+)/i);return {name:document.getElementById('name').value.trim(),category:document.getElementById('category').value.trim(),city:document.getElementById('city').value.trim(),website:document.getElementById('website').value.trim(),phone:document.getElementById('phone').value.trim(),notes,rating:current.rating??(ratingMatch?Number(ratingMatch[1]):null),reviews:current.reviews??(reviewsMatch?Number(reviewsMatch[1]):null)}}
async function analyzeLead(){const button=document.getElementById('analyzeLeadBtn');const reasons=document.getElementById('reasons');const priority=document.getElementById('priority');const score=document.getElementById('score');if(!button||!reasons||!priority||!score){alert('Analysis panel is not loaded correctly. Please refresh the page.');return}if(scoreRequestInFlight)return;current=payload();if(!current.name){reasons.textContent='Business name required';priority.textContent='Waiting';return}scoreRequestInFlight=true;button.disabled=true;button.textContent='Analyzing…';priority.textContent='Analyzing…';reasons.textContent='Sending lead to scoring engine…';try{const x=await api('/api/score',{method:'POST',body:JSON.stringify(current)});if(!x||x.ok!==true)throw new Error(x?.error||'Score analysis failed');score.textContent=String(x.score ?? '—');priority.textContent=(x.priority||'Unknown')+' priority';reasons.innerHTML=(Array.isArray(x.reasons)?x.reasons:[]).map(r=>'• '+escapeHtml(r)).join('<br>')||'Analysis completed.';const ai=document.getElementById('ai');if(ai)ai.textContent=''}catch(e){score.textContent='—';priority.textContent='Error';reasons.textContent='Analysis error: '+e.message}finally{scoreRequestInFlight=false;button.disabled=false;button.textContent='Analyze Lead'}}
async function generateAI(){if(!current.name)current=payload();if(!current.name){alert('Business name required');return}const button=document.getElementById('generateAiBtn');const old=button.textContent;button.disabled=true;button.textContent='Generating…';document.getElementById('ai').textContent='Generating AI sales analysis…';try{const x=await api('/api/gemini',{method:'POST',body:JSON.stringify(current)});document.getElementById('ai').textContent=x.ok?x.text:'AI: '+(x.error||'Unknown error')}catch(e){document.getElementById('ai').textContent='AI error: '+e.message}finally{button.disabled=false;button.textContent=old}}
function setActive(id){document.querySelectorAll('nav button').forEach(b=>b.classList.remove('active'));const el=document.getElementById(id);if(el)el.classList.add('active')}
function showDashboard(){setActive('navDashboard');document.getElementById('leadFinder').style.display='';document.querySelector('.grid').style.display='';document.getElementById('workflowPanel').style.display='';document.getElementById('moduleView').style.display='none';document.getElementById('ai').style.display='';window.scrollTo({top:0,behavior:'smooth'})}
function focusLeadFinder(){setActive('navLeads');showDashboard();document.getElementById('leadFinder').scrollIntoView({behavior:'smooth'})}
async function searchLeads(){const category=document.getElementById('searchCategory').value.trim();const city=document.getElementById('searchCity').value.trim();const limit=Number(document.getElementById('searchLimit').value);if(!category||!city){document.getElementById('leadMessage').textContent='Enter both niche and city.';return}document.getElementById('leadMessage').textContent='Searching…';try{const x=await api('/api/leads/search',{method:'POST',body:JSON.stringify({category,city,limit})});if(!x.ok){document.getElementById('leadMessage').textContent=x.error||'Lead search failed.';return}document.getElementById('leads').textContent=x.count;document.getElementById('missing').textContent=x.leads.filter(l=>l.website_missing).length;document.getElementById('high').textContent=x.leads.filter(l=>l.website_missing).length;document.getElementById('leadMessage').textContent=`Found ${x.count} prospects. No-website leads are the first sales targets.`;document.getElementById('leadTable').innerHTML=x.leads.map(l=>`<div class="lead-row"><div><strong>${escapeHtml(l.name)}</strong><small>${escapeHtml(l.address||city)}</small></div><div>${l.website_missing?'<span class="badge hot">No website</span>':'<span class="badge">Website</span>'}</div><div>${l.rating?`★ ${l.rating} (${l.reviews||0})`:'—'}</div><button class="secondary small" onclick='loadLead(${JSON.stringify(l)})' type="button">Analyze</button></div>`).join('')}catch(e){document.getElementById('leadMessage').textContent='Lead search error: '+e.message}}
function loadLead(l){current={name:l.name,category:l.category,city:l.address||'',website:l.website||'',phone:l.phone||'',notes:`Google rating: ${l.rating||'N/A'}; reviews: ${l.reviews||0}`,rating:typeof l.rating==='number'?l.rating:null,reviews:typeof l.reviews==='number'?l.reviews:null};document.getElementById('name').value=current.name;document.getElementById('category').value=current.category;document.getElementById('city').value=current.city;document.getElementById('website').value=current.website;document.getElementById('phone').value=current.phone;document.getElementById('notes').value=current.notes;document.getElementById('score').textContent='—';document.getElementById('priority').textContent='Waiting';document.getElementById('reasons').textContent='Lead loaded. Click Analyze Lead to score it.';window.scrollTo({top:document.body.scrollHeight/2,behavior:'smooth'})}
function showModule(title,subtitle,body){setActive(title==='SEO Audits'?'navAudits':title==='Demo Builder'?'navDemo':'navOutreach');document.getElementById('leadFinder').style.display='none';document.querySelector('.grid').style.display='none';document.getElementById('workflowPanel').style.display='none';document.getElementById('ai').style.display='none';const root=document.getElementById('moduleView');root.style.display='block';root.innerHTML=`<div class="panel module-panel"><div class="panel-head"><h2>${escapeHtml(title)}</h2><span>${escapeHtml(subtitle)}</span></div>${body}</div>`;window.scrollTo({top:0,behavior:'smooth'})}
function showAudits(){showModule('SEO Audits','Technical + local opportunity scan',`<p class="module-copy">Review the selected prospect for website, technical SEO, local SEO, and conversion opportunities.</p><div class="module-grid"><div><b>Business</b><span>${escapeHtml(current.name||'No lead selected')}</span></div><div><b>Website</b><span>${escapeHtml(current.website||'No website supplied')}</span></div><div><b>Category</b><span>${escapeHtml(current.category||'—')}</span></div><div><b>Location</b><span>${escapeHtml(current.city||'—')}</span></div></div><button class="primary" id="runAuditBtn" onclick="runAudit()" type="button">Run Audit</button><div id="auditResult" class="module-result">Run the audit to see live website findings.</div>`)}
async function runAudit(){if(!current.name)current=payload();if(!current.name){alert('Select or enter a business first.');return}const result=document.getElementById('auditResult');const button=document.getElementById('runAuditBtn');if(!result)return;if(!current.website){result.innerHTML='<h3>Website audit unavailable</h3><p>This lead has no website URL. Use the no-website opportunity from Lead Finder as the sales finding.</p>';return}if(button){button.disabled=true;button.textContent='Auditing website…'}result.textContent='Fetching live website and checking technical SEO signals…';try{const x=await api('/api/audit',{method:'POST',body:JSON.stringify(current)});if(!x.ok)throw new Error(x.error||'Audit failed');const m=x.metrics||{};const issueList=(x.issues||[]).map(v=>`<li>${escapeHtml(v)}</li>`).join('')||'<li>No major snapshot issues detected.</li>';const strengthList=(x.strengths||[]).map(v=>`<li>${escapeHtml(v)}</li>`).join('')||'<li>No additional strengths detected.</li>';result.innerHTML=`<div class="audit-summary"><h3>SEO Snapshot Score: ${escapeHtml(x.score)}</h3><p><strong>${escapeHtml(x.priority)} priority</strong> · HTTP ${escapeHtml(x.status_code)} · ${escapeHtml(x.response_time_ms)} ms</p></div><div class="module-grid"><div><b>Title</b><span>${escapeHtml(m.title||'Missing')} (${escapeHtml(m.title_length)})</span></div><div><b>Meta Description</b><span>${escapeHtml(m.meta_description||'Missing')}</span></div><div><b>H1</b><span>H1: ${escapeHtml(m.h1_count)} detected</span></div><div><b>Mobile</b><span>${m.viewport?'Viewport detected':'Viewport missing'}</span></div><div><b>Canonical</b><span>${escapeHtml(m.canonical||'Not detected')}</span></div><div><b>Crawl Files</b><span>robots.txt: ${m.robots?'OK':'Missing'} · sitemap.xml: ${m.sitemap?'OK':'Missing'}</span></div><div><b>Content</b><span>${escapeHtml(m.word_count)} words</span></div><div><b>Links</b><span>${escapeHtml(m.internal_links)} internal · ${escapeHtml(m.external_links)} external</span></div></div><div class="audit-columns"><div><h4>Issues to Fix</h4><ul>${issueList}</ul></div><div><h4>Strengths</h4><ul>${strengthList}</ul></div></div><p class="module-copy">${escapeHtml(x.note||'')}</p>`}catch(e){result.innerHTML=`<h3>Audit failed</h3><p>${escapeHtml(e.message)}</p>`}finally{if(button){button.disabled=false;button.textContent='Run Audit'}}}
function showDemoBuilder(){showModule('Demo Builder','Client-specific landing page foundation',`<p class="module-copy">Create a client-specific demo brief from the selected prospect and its latest SEO audit. No Gemini dependency is required.</p><div class="module-grid"><div><b>Business</b><span>${escapeHtml(current.name||'No lead selected')}</span></div><div><b>Category</b><span>${escapeHtml(current.category||'—')}</span></div><div><b>Location</b><span>${escapeHtml(current.city||'—')}</span></div><div><b>Website</b><span>${escapeHtml(current.website||'No website')}</span></div></div><button class="primary" id="generateDemoBtn" onclick="generateDemoBrief()" type="button">Generate Demo Brief</button><div id="demoResult" class="module-result"></div>`)}
function buildDemoBrief(){const notes=(current.notes||'').toLowerCase();const noWebsite=!current.website;const rating=typeof current.rating==='number'?current.rating:null;const reviews=typeof current.reviews==='number'?current.reviews:null;const seoFocus=['Local landing page','Service pages','Google Business Profile alignment'];const opportunities=[];if(noWebsite){opportunities.push('Create a fast mobile-first website with clear click-to-call and quote CTAs.','Build dedicated service and location pages for local search demand.')}else{opportunities.push('Improve conversion paths with prominent click-to-call and quote CTAs.','Expand local service-area and service pages around priority search locations.')}if(rating!==null&&rating<4.5)opportunities.push(`Build a review-generation plan to improve the current ${rating.toFixed(1)}-star reputation.`);if(reviews!==null&&reviews<100)opportunities.push(`Increase review volume from the current ${reviews} reviews.`);return {business:current.name,category:current.category||'',location:current.city||'',current_website:current.website||'None found',hero_cta:noWebsite?'Get a Free Quote':'Request Service',recommended_sections:['Hero','Services','Why Choose Us','Reviews','Service Area','Contact','FAQ'],seo_focus:seoFocus,lead_signals:{website_missing:noWebsite,rating,reviews},demo_opportunities:opportunities,audit_note:'Use the selected lead and latest SEO Audit findings to customize the final demo.'}}
function generateDemoBrief(){const button=document.getElementById('generateDemoBtn');const result=document.getElementById('demoResult');if(!current.name){alert('Select a lead first.');return}if(button){button.disabled=true;button.textContent='Generating…'}try{const brief=buildDemoBrief();result.innerHTML=`<div class="audit-summary"><h3>Demo Brief Ready</h3><p>Client-specific foundation generated successfully.</p></div><pre>${escapeHtml(JSON.stringify(brief,null,2))}</pre>`;demoCount+=1;updateDemoCounter()}catch(e){result.innerHTML=`<h3>Demo generation failed</h3><p>${escapeHtml(e.message)}</p>`}finally{if(button){button.disabled=false;button.textContent='Generate Demo Brief'}}}
function showOutreach(){showModule('Outreach','Personalized draft, human approval',`<p class="module-copy">Prepare a personalized outreach message using the selected lead, score, audit, and demo opportunities. Sending is not automated; you review and send it yourself.</p><label>Subject</label><input id="outreachSubject" class="module-input" value="Quick growth idea for ${escapeHtml(current.name||'your business')}"><label>Message</label><textarea id="outreachMessage" class="module-textarea">Hi ${escapeHtml(current.name||'there')},\n\nI was reviewing local businesses in your area and noticed an opportunity to improve your online presence. I can prepare a quick SEO and website improvement outline tailored to your business.\n\nBest,\nIdeal SEO Agency</textarea><button class="primary" onclick="prepareOutreach()" type="button">Prepare Draft</button><div id="outreachStatus" class="module-result"></div>`)}
function prepareOutreach(){document.getElementById('outreachStatus').textContent='Draft prepared. Review manually before sending.'}
function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
init();